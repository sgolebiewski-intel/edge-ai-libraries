
configure_uv () {
  echo "$PATH" | grep -q -F ~/.local/bin || export PATH=$PATH:~/.local/bin
}

debian_30_profile_uv () {
  echo "pipx"
}

debian_30_install_uv () {
  configure_uv
  if uv --version 2> /dev/null && [[ " $* " != *" --reinstall "* ]]; then
    echo "Uv is already installed. Skipping..."
  else
    pipx install uv
  fi
}

