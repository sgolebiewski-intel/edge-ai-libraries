
configure_edge_pack_npu () {
  configure_edge_pack "$@"
  version="1.0"
}

debian_20_profile_edge_pack_npu () {
  echo "edge_pack"
}

debian_20_install_edge_pack_npu () {
  configure_edge_pack_npu "$@"
  if ensure_apt_is_installed intel-edge-npu=$version && [[ " $* " != *" --reinstall "* ]]; then
    echo "intel-edge-npu already installed. Skipping" 1>&2
  else
    debian_20_remove_edge_pack_npu
    ensure_apt install --no-install-recommends "intel-edge-npu=$version*"
  fi
}

debian_20_remove_edge_pack_npu () {
  ensure_apt autoremove -y intel-edge-npu || true
}

debian_20_sbom_edge_pack_npu () {
  configure_edge_pack_npu "$@"
  IFS=$'|'
  local pref_str="${edge_pack_pref[*]}"
  cat <<EOF
"name": "$edge_pack_repo"
"gpg-key": "$edge_pack_key_url"
"key_file": "$edge_pack_key_file"
"apt-source": "$edge_pack_source"
"apt-list-file": "$edge_pack_list_file"
"apt-pref": ["${pref_str//|/'","'}"]
"apt-pref-file": "$edge_pack_pref_file"
"pkg-list": ["intel-edge-npu=$version*"]
EOF
}

